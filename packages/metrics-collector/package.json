{
	"name": "seaquel-metrics-collector",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev --test-scheduled",
		"start": "wrangler dev --test-scheduled",
		"seed:local": "node scripts/seed-local-kv.mjs",
		"cf-typegen": "wrangler types",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"devDependencies": {
		"@types/node": "^25.3.0",
		"typescript": "^5.5.2",
		"vitest": "^4.1.7",
		"wrangler": "^4.67.0"
	}
}
