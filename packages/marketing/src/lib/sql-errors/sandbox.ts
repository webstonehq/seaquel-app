/**
 * Re-exported from the shared sandbox, which /learn-sql challenges also use.
 * Edit the runner in $lib/sandbox/index.ts.
 */
export { getDatabase, runQuery, type RunResult } from '$lib/sandbox';
